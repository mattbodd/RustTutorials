// The main function will always run fist
// Takes in no parameters
fn main() {
    // Rust uses spaces NOT tabs
    // The ! indicates that println is a Rust macro
    //
    println!("Hello, world!");
}
